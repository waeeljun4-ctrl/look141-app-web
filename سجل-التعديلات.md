# look.141 — سجل التعديلات والمرجع السريع

---

## البيئة والمسارات

| الأداة | القيمة |
|--------|--------|
| الموقع | `C:\laragon\www\look141-app` |
| التطبيق المحمول | `C:\laragon\www\look141-mobile` ← **جديد** |
| الرابط المحلي (نفس الجهاز) | `http://localhost:8001` |
| الرابط من الموبايل (نفس شبكة الواي فاي) | `http://192.168.1.51:8001` |
| قاعدة البيانات | `look141` (MySQL, user `root`, بدون password) |
| PHP 8.3 | `C:\laragon\bin\php\php-8.3.30-Win32-vs16-x64\php.exe` |
| Node v22 | `C:\laragon\bin\nodejs\node-v22` |

### تشغيل السيرفر (للاختبار من الموبايل)

```powershell
$phpExe = "C:\laragon\bin\php\php-8.3.30-Win32-vs16-x64\php.exe"
cd "C:\laragon\www\look141-app"
& $phpExe artisan serve --host=0.0.0.0 --port=8001
```

### الستاك
Laravel 11 · PHP 8.3 · MySQL · React 18 · Inertia.js v2 · Tailwind CSS v3 · Vite 5

### الميزات الموجودة فعلياً بالكود (أكثر تقدماً من luxsign-app بنواحي)
- `Api/AuthController`, `Api/ProductApiController`, `Api/OrderApiController` — Mobile API
- `ChatController`, `PricingController`
- `SanitizeInput`, `SecurityHeaders` middlewares
- `Brand` model + `BrandController`, `ProductVariant` model
- i18n كامل: `i18n.js` + `LocaleContext.jsx` + `LangPicker` (ar/he/en)
- `WishlistContext.jsx` / `WishlistDrawer.jsx`

---

## سجل التعديلات — 2026-07-06 (جلسة الموبايل/RTL)

### الملفات المتأثرة
- `resources/js/Pages/Store/Index.jsx`
- `resources/js/Layouts/AdminLayout.jsx`

### شو تم
1. **القائمة الجانبية بلوحة الإدارة على الموبايل** — كانت `fixed` بعرض ثابت بدون استجابة، تغطي الشاشة بالكامل. أضفنا حالة فتح/إغلاق + زر ☰ بالهيدر + خلفية غامقة (overlay) تسكر القائمة عند الضغط + `lg:` breakpoint تخليها ظاهرة دايماً على الشاشات الكبيرة.
2. **صندوق البحث بالموبايل** — كان مزنوق بنفس صف الشعار والأيقونات (النص يوصل مقطوع زي "ابحث عن منتج أو مار..."). صار بسطر مستقل تحت شريط التنقل، وتصميمه تحوّل من صندوق (border+bg) لخط سفلي minimal شفاف يظهر بلون accent فقط عند التركيز.
3. **زر الإدارة ⚙️** كان مخفي بالكامل على الموبايل (`hidden sm:flex`) — صار ظاهر بكل أحجام الشاشات.
4. **ترتيب شريط التنقل (RTL)** — كانت الأيقونات (مفضلة، لغة، إدارة، سلة) على الشمال والشعار `look.141` على اليمين. عكسناها: الأيقونات صارت يمين، الشعار شمال، يطابق قراءة العربي من اليمين لليسار.
5. **العرض/التخطيط** — أزلنا `max-w-7xl` من حاوية الشريط الجانبي + المحتوى الرئيسي، المحتوى صار يملأ عرض الشاشة بالكامل بدل ما يضل بمنتصف الشاشة بهوامش فاضية على الشاشات العريضة.

### ⚠️ تحذير مهم لأي جلسة قادمة

**لا تستخدم PowerShell `Get-Content`/`Set-Content` لتعديل أي ملف فيه نص عربي/عبري أو إيموجي.** بدون `-Encoding UTF8` صريحة على القراءة، PowerShell 5.1 بيقرأ بترميز خاطئ ويحوّل كل حرف متعدد البايت لرموز مشوّهة (mojibake) بشكل دائم. استخدم أداة Edit/Write حصراً.

---

## سجل التعديلات — 2026-07-06 (تطبيق موبايل جديد)

أُنشئ تطبيق موبايل جديد لـ look.141 بمسار `C:\laragon\www\look141-mobile`، مبني بنفس بنية `luxsign-mobile` تماماً (Expo React Native · React Navigation · Axios · Sanctum Bearer) لكن مُكيَّف مع شكل بيانات look141 الفعلي (variants بالمقاس/اللون بدل أنواع التسعير الثمانية، brand، compare_price).

### البنية
```
look141-mobile/
  App.js                          ← Tab navigator: المتجر / السلة / حسابي / (الإدارة لو admin)
  src/
    api.js                        ← axios + Bearer token من AsyncStorage
    config.js                     ← API_BASE = http://192.168.1.51:8001، ألوان accent/ink/cream
    context/AuthContext.js        ← تسجيل/دخول/خروج، /api/auth/*
    context/CartContext.js        ← سلة بمفتاح name+size+color (وليس name فقط زي luxsign)
    screens/store/StoreScreen.js  ← شبكة منتجات + فلترة صنف + بحث + خصم %
    screens/store/ProductScreen.js← اختيار مقاس/لون حسب stock الـ variant + كمية
    screens/store/CartScreen.js   ← فورم الطلب (اسم/هاتف/مدينة/عنوان) → POST /api/orders
    screens/ProfileScreen.js      ← حساب المستخدم / تسجيل خروج
    screens/admin/AdminOrdersScreen.js ← لائحة الطلبات + تغيير الحالة (admin فقط)
```

### فروقات متعمّدة عن luxsign-mobile
- `CartContext.addItem(product, {size, color}, qty)` بدل `addItem(name, icon, price, category)` — لأنه نفس المنتج بمقاسين مختلفين لازم يكونوا سطرين منفصلين بالسلة.
- `ProductScreen` يعرض أزرار مقاس ثم لون (حسب توفر الـ stock لكل تركيبة) بدل حاسبة الأنواع الثمانية للتسعير.
- كلمة مرور التسجيل: حد أدنى 8 أحرف (يطابق `Api/AuthController@register` بـ look141-app، luxsign كان 6 قبل ما تحدَّث).

### التحقق
- `npm install` نجح (513 حزمة).
- `npx expo start` شغّل Metro Bundler بدون أخطاء، وطلب bundle كامل لـ iOS نجح (1014 module، ~5.3MB) — يتأكد من عدم وجود أخطاء syntax/import بكل الملفات.
- Git: `git init` + أول commit (23 ملف، `node_modules` مستثنى بالـ `.gitignore`).

### ⚠️ ملاحظات مهمة
1. **الأيقونات/splash screen مؤقتة** — انتسخت من `luxsign-mobile/assets` كـ placeholder فقط (نفس الشعار الذهبي القديم). لازم تُستبدل بأيقونات look.141 الفعلية قبل أي بناء نهائي (`icon.png`, `android-icon-*.png`, `favicon.png`, `splash-icon.png`).
2. **التشغيل للتجربة من الموبايل:**
   ```powershell
   $env:PATH = "C:\laragon\bin\nodejs\node-v22;" + $env:PATH
   cd "C:\laragon\www\look141-mobile"
   npx expo start
   ```
   ثم افتح تطبيق **Expo Go** على الموبايل وامسح الـ QR code (لازم يكون الموبايل عالشبكة نفسها والباك إند `look141-app` شغّال على `php artisan serve --host=0.0.0.0 --port=8001`).
